# セキュリティポリシー

## サポート対象

**最新の公開版のみ**を対象とします。過去の版へ修正をバックポートしません。

## 脆弱性の報告

**公開Issueでは報告しないでください。**

[Security Advisories](https://github.com/nulogic-jp/cozeni-sdk/security/advisories/new) から
非公開で報告してください。次の内容が含まれていると調査が早く進みます。

- 影響を受けるバージョン
- 再現手順
- 想定される影響（認可の回避、トークンの漏洩、なりすましなど）

報告に **APIキー・購入者トークン・本番のCookie値そのものは含めないでください。**
再現に必要な場合は、値を伏せた形で構造だけを示してください。

初回の応答は5営業日以内を目安とします。

## このSDKが扱うもの

このSDKはサーバー上で動作し、以下を扱います。

- Cozeni外部APIのAPIキー（管理クライアント）
- 購入者のハンドオフコードとセッショントークン
- 購入者セッションのCookie（`cozeni_customer`）と、ハンドオフ直後の印（`cozeni_handoff`。秘密を含まない）

そのため、ブラウザでの実行は明示的に拒否しています。
APIキーをクライアントサイドのバンドルへ含めないでください。

同梱のCLI（`npx @nulogic/cozeni-sdk`）は、ログインで得たAPIキーを利用者のホームディレクトリ
（`$XDG_CONFIG_HOME/cozeni/`、既定は `~/.config/cozeni/`）に平文で保存します。
ディレクトリ0700・ファイル0600で書き、シンボリックリンクや緩い権限を見つけたら読み書きしません。
キーは発行された接続先にだけ送り、リダイレクトを追いません。キーの権限は商品と購入リンクの操作に限られ、
発行から30日で失効します。このファイルをリポジトリや本番環境へ持ち出さないでください。
同じディレクトリの `config.json`（既定のプロファイルと、使うクリエイター・接続先。秘密は含まない）も同じ権限で書きます。
使うキーのクリエイターが `config.json` の期待と違えば、変更系の要求を送る前に止まり、
別のアカウントで承認されたログインのキーは保存せずに失効させます。

`init` は、プロジェクトの package manager を子プロセスで実行して、この版のSDKを依存に追加します。
引数は固定の値だけで、出力は表示も保存もしません（認証情報を含みうるため）。
子プロセスの環境からは `COZENI_` で始まる変数（`COZENI_API_KEY` など）を外します。package manager の認証設定（npm のトークン等）は利用者のものなので残します。
lifecycle script は止めません（`--ignore-scripts` にすると、プロジェクトの他の依存のインストールが壊れうるため）。SDK 自身は install 時のスクリプトを持ちません。
同梱のskillはプロジェクトへコピーし、シンボリックリンクは作りません。置き場所の途中がシンボリックリンクか、プロジェクトの外を指していれば書き込みません。
